# v66.0.2 Git Remote Allowlist Evidence

Expected result:

- `git@github.com` false positives removed from public gate.
- Real emails remain detectable.
- Scanner no longer crashes on external diagnostic paths.
- Vercel readiness can report `privacy_gate_ok: true` only when no other public findings remain.
