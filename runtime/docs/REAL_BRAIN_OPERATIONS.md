# Real Brain Operations Layer

PACK3 turns the runtime into a practical local operations layer. It can create inbox items, route context, search memory, produce project snapshots, generate report cards, record approval requests and export a bundle.

Core rule: every operation remains local-first, evidence-aware and human-controlled.
