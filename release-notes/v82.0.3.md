# v82.0.3 — Public Nav Cleanup Finalizer-Safe Repair

Fixes visible `\n` public-navigation issue and adds a finalizer-safe capture flow to avoid repeated red endings from generated reports or dirty-tree leftovers.
