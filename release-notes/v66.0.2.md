# v66.0.2 — Privacy Allowlist / Git Remote False Positive Cleanup

This release fixes the `git@github.com` false positive in the privacy firewall by allowlisting it only as Git SSH remote metadata.

It also hardens `rel(path)` so scanner diagnostics can safely scan temporary files outside the repository.

Real email detection remains active.
