# Public Export Scanner

Command:

```bash
runtime/bin/privacy-firewall public-gate
```

R2 continue captures findings into `brain/privacy/audit/public-gate-report.json` without hiding the fact that findings exist.
