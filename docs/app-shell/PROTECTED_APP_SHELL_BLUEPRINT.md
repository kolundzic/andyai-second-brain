# Protected App Shell Blueprint

Route direction:

- `/app/` protected workspace shell
- `/login/` login prototype
- `/access-denied/` blocked access explanation
- `/dialog/` public prototype remains demo
- `/capture/` public prototype remains demo

Protected app shell areas:

- workspace overview
- ask brain panel
- upload intake entry
- memory list placeholder
- audit activity placeholder
- approval queue placeholder
- tenant/user status badge
