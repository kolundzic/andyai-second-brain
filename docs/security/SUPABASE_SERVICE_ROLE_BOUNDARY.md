# Supabase Service Role Boundary

The service role key is a root-level database capability.

It must never be exposed to public HTML, browser JavaScript, screenshots, README examples with real values, client logs, public Vercel env vars, or generated static exports.

Use service role only in trusted backend/operator scripts.
