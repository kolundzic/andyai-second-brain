# Metadata Instruction Detector Spec

Metadata includes filenames, titles, captions, frontmatter, JSON fields and comments.

Metadata is descriptive data, not trusted instruction.

If metadata contains operational language such as run, execute, ignore, override or deploy, it must be flagged.
