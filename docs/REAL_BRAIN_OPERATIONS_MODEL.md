# Real Brain Operations Model

The brain runtime now follows a simple loop:

Inbox → Route → Classify → Create Card → Link Evidence → Request Approval if Needed → Update Lifecycle → Report → Export

This is not autonomous production action. It is local structured memory operation with human visibility.
