# Operator / Admin Split

Admin manages workspace settings and users.
Operator executes approved workflow operations.

Operator cannot bypass:

- tenant boundary
- RLS
- approval gate
- privacy firewall
- logic-layer firewall
