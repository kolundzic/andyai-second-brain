# Operator Help Index

Daily checks: `git status --short`, `scripts/verify.sh`, `scripts/brain-check.sh`.

Public route checks: `/`, `/help/`, `/knowledge/`, `/dialog/`, `/capture/`, `/trust/`, `/faq/`, `/health/`.

Safety checks: privacy firewall, logic-layer firewall, human approval before public/client delivery.
