# Mobile Camera Scan Spec

The mobile camera layer lets the user scan paper documents into Second Brain.

Use cases:

- receipts
- contracts
- handwritten notes
- meeting notes
- letters
- business cards
- printed reports
- whiteboard snapshots

Future pipeline:

Camera capture → image cleanup → OCR → key field extraction → classification → memory attach → analysis → report.
